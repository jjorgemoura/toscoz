name: PR Versioning

on:
  pull_request:
    types: [ labeled, unlabeled ]

jobs:
  labelTagVersion:
    name: Label Tag Version
    runs-on: macOS-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Bundle Install 
      run: bundle update --bundler

    - name: ReadMessage_Patch
      if: ${{ contains( github.event.pull_request.labels.*.name, 'bump:patch') }}
      run: bundle exec fastlane save_version_token version_tag:patch

    - name: ReadMessage_Minor
      if: ${{ contains( github.event.pull_request.labels.*.name, 'bump:minor') }}
      run: bundle exec fastlane save_version_token version_tag:minor

    - name: ReadMessage_Major
      if: ${{ contains( github.event.pull_request.labels.*.name, 'bump:major') }}
      run: bundle exec fastlane save_version_token version_tag:major
